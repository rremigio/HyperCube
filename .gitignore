# Python caches / build artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# OS cruft
.DS_Store

# Data cubes, fit outputs, and sessions (too large / user-specific for the repo)
*.fits
*.hcsession
*.csv

# Bundled stellar template libraries (ship separately, not via git)
eMILES/
indo_us_library/

# Keep small example/config CSVs explicitly if needed by un-ignoring, e.g.:
# !example_config.csv
